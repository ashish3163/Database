

1) Which Field  of the customers table is the primary key?

--> CNUM is primary key

2)What is the 4th column of the customer table?

--> COMM is 4th column of customer table

3)What is another word for row?For column?

-->      Dbms                  Rdbms
Row-----Record                 Tuple,row,entity,oppertunity

column--field                  column,attribute,method


4)Why isn't possible to see the first  five rows of a table?

-->No,not possible to see first five rows.
    when we upadte a row if the row length is incresing then the entire may be moved some other address ,later we select from the table we will find row at some other opsition int the output.due to these we can't see the first five rows.